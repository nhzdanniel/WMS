<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wms">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WMS">
        <activity android:name=".GenerateIncomingPOActivity"></activity>
        <activity android:name=".ViewPOActivity" />
        <activity android:name=".ManageAccountsActivity" />
        <activity android:name=".ViewInventoryReportsActivity" />
        <activity android:name=".ViewProductsActivity" />
        <activity android:name=".HomePageActivityWM" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>